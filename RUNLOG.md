# RUNLOG — chronological, append only

## 2026-09-10
- 00:47 Freeze package unpacked to repo root; `.env` created and confirmed gitignored; commit `332619c` "Bedside Brief: Day 1 freeze package" pushed to `claude/git-repo-setup-rvdx9z`.
- 00:50 Pre-flight FAILED: egress proxy 403 on api.openai.com and all literature hosts (Trusted network level). Reported to owner.
- 00:58 Owner switched environment to Full. Re-test: `gpt-4.1` one-token call OK (10 tokens); pubmed 200, doi.org 301, crossref 302 reachable; jamanetwork.com 403 (publisher bot block, see DECISIONS #5).
- 01:00 Phase 0 started. Launched 3 discriminator-id drafting agents (cardio/volume; shock/renal/neuro; abdomen/fever/weakness) and 3 case-author agents (4 presentations each).
- 01:20 Case-author ×3 done: 36 base cases (108 inputs) written blind, validator 0 errors; committed 3d65e36.
- 01:30 Id drafts ×3 done (75/50/46) → merged 148 ids, 5 duplicates folded; committed b419c32.
- 01:35 Phase 2 deterministic modules + 30 tests (builder agent) committed 9516ecd.
- 01:45 Case-critic ×2 done: 20 pass / 16 revise. Revisions dispatched to the original authors (DECISIONS #11–15).
- 02:20 Author revisions ×3 applied (commit 09cc20b). Critic re-check: 26 pass / 10 fix → 11 exact edits applied by coordinator; validator 0 errors.
- 02:25 LLM modules (llm/parser/rank/pipeline/api) + 24 tests landed (54 total, all green); live startup_check → gpt-4.1. Commit 8bff84c.
- 02:30 PHASE0_GATE.md generated; owner gate opened. Phase 1 cardiology extraction starting in background.
- 03:40 Owner gate rulings received (DECISIONS #18); vocab cross-listings, 5 palpitations ids, compound splits, chest_pain_002 re-branch applied. Critic split re-check running.
- 03:45 Wave 1 (24 ids) + wave 2 (34 ids) extraction complete: 116 packets, validator 0 errors; status 72 quantified / 38 partial / 6 not_quantified; diff: 2 agree, 42 partial, 14 disagree. Red-team wave 1 running. verify_ui.py built (9 tests). Wave 3 (37 ids: shock/renal/neuro/abdomen/AMS + 2 palpitations) launched.
- 02:10 Rate limit killed 12 background agents (critic re-check, red-team ×2, eval builder, wave-3 extractors ×8). Critic's report had already been written to disk; agents resumed after reset (A-side extractors first to cap concurrency).
- 02:24 **Freeze #1**: content commit 39a698e; hash stamped into all 36 case files; freeze guard test added (37 tests). Benchmark is read-only from here except `reference_targets_mapped` at freeze #2.
- 02:55 Wave 3 complete: 190 packets over all 95 priority-1 ids. Red-team wave 1 committed (no fabricated numbers; direction/range/author/composite issues → prompt hardened, DECISIONS #23). Verification queue handed to owner (gate 2). Eval harness landed (155 tests). Wave 4 A-side (58 P2/P3 ids) + red-team wave 3 launched.
- 03:10 Red-team wave 2 committed (0 fabrication, 110/110 quotes verified). Sidecar + target map landed. **Freeze #2** = 28a5f2617dcd474602491e71823338316f18bc2d.
- 03:40 Freeze #2 (28a5f26). Arm C (generic LLM, gpt-4.1) run on all 108 frozen inputs as a DEV run (not the reportable run): 102/108 completed before the client had 429 backoff; preliminary, non-abstract signal: ~28 items per answer, bedside share ≈0.60, ~22% of items carry a numeric claim. Parser dry-run exposed out-of-vocab tags → corrective re-parse added (DECISIONS #27). Red-team wave 3 committed. Wave 4 A-side complete (58 ids); B-side running.
- 04:20 **Extraction complete**: 306 packets = 153 ids × 2 independent extractors; validator 0 errors. Red-team waves 1–3 committed; wave 4 launching. Parser: vocab corrective re-parse (108/108 parse) and `underspecified` flag to gate ask-first (DECISIONS #27, #29). Dev arm C run complete (108/108).
- 04:50 Red-team wave 4 complete (all 4 waves now committed; 153/153 ids reviewed). Code-review audit: 10/10 non-negotiables PASS with adversarial checks executed. Parser ask-first recalibrated (4/6 sensitivity, 0 false positives). PROGNOSTIC-unlabelled check added to the diff tool. Phase 1 extraction closed; owner verification is the critical path.

## 2026-09-11
- 08:10 **Owner verified all 153 records** over ~6 h in the iPad verifier (86 from packet B, 67 from A; 9 estimates excluded, 1 value corrected, 0 rejections).
- 08:20 Store built: 153 verified, 349 estimates, 12/12 presentations, 22.2% not_quantified. Validator 0 errors. Fixed a puller bug: excluding every estimate dropped the sources a `not_quantified` record still needs to cite.
- 09:40 **Post-promotion fix package prepared, not applied.** Re-read both extraction packets for all 9 red-team defects that survived into the verified store; diffs and rationale in `PROPOSED_FIXES.md`, implemented in `tools/apply_fixes.py` (dry run by default, idempotent, matches estimates on their own quote). Two citations re-fetched from PubMed and confirmed fabricated: "Beacock CJ" (`pocus_bladder_volume`, PMID 30325783 is Taylor DL et al.) and the Kharbanda author list (`exam_abdominal_rigidity_guarding`, PMID 17192449 is Becker T et al.); each appears in exactly one record. Corrected two things I had told the owner: only 6 empty estimates remain (not 11 — 5 were already excluded during promotion), and flipping packets is the wrong fix for 4 of the 9 because it discards good estimates. Trialled both option sets on a throwaway copy: 349 → 337 estimates, validator 0 errors, warnings 14 → 8, re-run makes no further change. Also fixed a real bug in `tools/validate.py`: the empty-estimate check tested the stat *dict* (always truthy) instead of its value, and ignored `prevalence`. Nothing in `records/verified/` has been touched — the owner promotes.
- 10:05 **Fix package applied** (owner approved flip-a / label / rewrite / apply). 15 records changed: 12 estimates deleted, 2 citations corrected against PubMed, 1 packet re-promoted, 1 estimate replaced by its traced primary, 1 estimate added, 1 interpretation rewritten, 2 rows labelled PROGNOSTIC. Store: 153 records, 349 → 337 estimates, 22.2% → 24.2% not_quantified, validator 0 errors (warnings 14 → 8), 204 tests green, re-running the fix script changes nothing. Gate 2 fully closed — the verified store is now the frozen evidence base for the eval. Next: arms A/B/C on the frozen set, then error analysis and the gate-3 judging queue.
- 10:35 **Run 1 complete and committed** (`eval/runs/20260911T084331Z`): 108/108 in all three arms, 0 errors. Must-have recall A 69.0% / B 66.0% / C 71.3%; within the 234 targets the library covers, A 73.1% vs C 72.6%; on the 27 with no record, A 14.8% vs C 63.0% — the recall gap is library coverage, not architecture. Unsupported numeric claims A 0.0% vs C 88.6%; noise stability 0.749 vs 0.095; brevity 100% vs 0%; items per card 6.1 vs 28.7. Two failure modes found in arm A: it recommended orthostatic vitals for a patient with supine SBP <90 in all three variants of `hypotension_002` (the case's own should_not_recommend), and `safety_scope` turned out to be read by nothing — `render.py` printed it and that was the whole of its use. Fixed by passing the digit-stripped `do_not_use_when` to the ranker (DECISIONS #43). Run 1 artifacts are now committed, raw per-case outputs included: the container is ephemeral and this is the evidence behind every abstract number. `.gitignore` reworked so a run's summaries are kept by default — the old `eval/runs/` rule excluded the directory itself, which would have made any negation inside it dead.
- 10:36 **Container restarted mid-session** and killed the run-2 background job at arm A 27/108; on-disk state survived intact. Restarted from where it stopped. Worth remembering: nohup'd work does not survive a container restart, so a long run needs its process checked, not just its output directory.
- 11:05 **Run 2 (`…102303Z-safety-fix`) complete: the contraindication fix did not work.** `should_not_recommend` hits 6 → 5 cases — only `hypotension_002_n1` cleared, while `hypotension_002` and `_p1` still put orthostatic vitals on the card — and must-have recall moved 69.0% → 68.3% (quantified stratum 73.1% → 70.1%, perturbation responsiveness 68.1% → 63.9%). Reproducing the ranker's input showed `avoid_when` reached the model verbatim; the patient summary was the problem. The one-liner states `BP 88/54` at rest and the parser rendered it as chief_complaint "lightheadedness and hypotension **on standing**", under which "do not attempt standing measurements in a patient who is already hypotensive" does not apply. Systematic: 27 of 36 one-liners state a vital sign and **none** of the 108 parses kept a single numeric vital — the prompt says "no numbers" and `parser.py` then `strip_numbers()` every field. Arms B and C reused from run 1 unchanged (neither ranks); only arm A was re-run.
- 11:25 **Root-cause fix: the parse now keeps the patient's own measurements.** New `observed_values` field, copied verbatim from the one-liner, not digit-stripped, passed to the ranker and never rendered. Containment is untouched — `render.py` strips every card-level string again and the validator blocks any number absent from a linked verified record, so the parse-level stripping was defence in the wrong place: it bought nothing the card did not already have and cost the ranker the patient's state. Affects the parser, so arms A and B both re-run; arm C reused. 210 tests green.
- 12:10 **Run 3 and run 4 complete; the picture is a 2×2 ablation.** Run 3 (vitals + avoid_when) is the only configuration with zero `should_not_recommend` violations, at recall 67.7% and perturbation 57.9%. Run 4 (vitals only, `BB_RANKER_CONTRAINDICATIONS=0`) is the best performer — recall 71.0%, quantified stratum 74.4%, perturbation 69.4%, cards back to 6.06 items — but its 3 violations are exactly the three `hypotension_002` variants, standing a patient at 88/54. Arm A's quantified-stratum recall of 74.4% now clearly exceeds arm C's 72.6%, and overall recall 71.0% ties C's 71.3%. Arm B flat across all four runs, which is what makes the attribution to ranking sound. Also fixed the `should_not_recommend` matcher (3 of run 1's 6 arm-A hits were false positives on "NSAIDs for muscle pain") and recomputed every run. Which configuration ships is an owner decision on clinical risk tolerance.
- 23:45 **Run 5 (`…231224Z-vitals-relevant-contra`) complete: zero violations at 70.0% recall / 65.3% perturbation.** The deterministic gate (`bedside_brief/patient.py`, 19 unit tests) recovers most of what the blanket caveat cost — perturbation 57.9% → 65.3%, recall 67.7% → 70.0% — while keeping `should_not_recommend` at 0 cases. Five-run ablation now complete and committed. Recommending run 5's configuration over the shipped `"off"` default; owner decision pending. Error analysis written (`ERROR_ANALYSIS.md`): of 88 missed must-have targets, 50% lost the 8-slot competition, 25% were never offered by retrieval, 25% have no record — which revises the library-expansion answer downward.
- 00:20 **Gate 3 handed to the owner.** Blinded judging page published with the `db` capability (`tools/web/judge.html`); the queue is seeded as 13 documents (12 cases + index) from `tools/seed_judging.py`, scored on the page, pulled back by `tools/pull_judging.py` and merged by `eval.judging_import`. 525 items over 12 cases, one per presentation, from run 5. Items are ordered by uid hash within a case rather than by arm, so the sequence a judge sees mixes the arms (syncope_001: longest same-arm run 4, mean 1.57). The arm lives only in `judging_key.json`, which the page never receives. Browser-checked before sending this time — and the check caught a real bug: `DocumentSnapshot.data` is a function in the runtime contract and the page was reading it as a property, which my first stub had mirrored, so it passed a test that was wrong. Fixed both; resume from saved scores verified separately. Full pipeline smoke-tested through to `judged_metrics.md`.
- 09:10 **Gate 3 efficiency pass.** The rubric and the fast flow now live in the page itself (collapsible panel) rather than in a separate document, and the bulk action moved into the sticky bar since the recommended flow ends with it. Per-case load: 26–48 judgements, 427 total. The operational rule that matters is drift, not strictness: judged relevance is a between-arm comparison, so systematic strictness cancels while drifting strictness does not, and every case contains all three arms — so a case finished in one sitting keeps any drift arm-neutral. Deliberately did NOT tell the owner the arm composition per case (68% of groups are single-arm C), because that would prime the blinded judgement.
