Drop `bedside-brief-handoff.zip` into the repo root, then paste this into Claude Code:

---
There is a file bedside-brief-handoff.zip in this repo. Unzip it, move the contents of the bedside-brief/ folder to the repo root (delete the zip and the empty folder), and make an initial commit "Bedside Brief: Day 1 freeze package". Then read CLAUDE.md and PLAN.md and work autonomously through PLAN.md starting at Phase 0. Use subagents as described in CLAUDE.md (extractor-A, extractor-B, red-team, case-author, case-critic, code-reviewer). Do not stop for questions except at the owner gates listed in CLAUDE.md; batch anything else into DECISIONS.md. Keep PLAN.md checkboxes and RUNLOG.md current. Before the first commit, add `.env` to .gitignore. Read OPENAI_API_KEY from the .env file in the repo root, verify it works with a one-token test call, and confirm subagents can reach the web for PubMed/DOI lookups; if either is missing, tell me that first and nothing else. Never print or commit the key. POCUS: leave the default in DAY1_FREEZE.md (extract but disabled in retrieval). When you reach the Phase 0 owner gate, present: discriminator_ids.json, any vocab.json changes, and 6 sample benchmark cases, with a numbered list of exactly what you need me to decide. Then continue Phase 1 extraction in the background while waiting for my sign-off, cardiology first. Go.
---
