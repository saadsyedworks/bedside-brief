# DECISIONS — batched, one line each, newest at bottom

| # | Date | Decision | Rationale |
|---|------|----------|-----------|
| 1 | 2026-09-10 | LLM model = `gpt-4.1` (resolved `gpt-4.1-2025-04-14`), provider `openai`, verified with a one-token call at kickoff. | CLAUDE.md default; key has access. Fallback `gpt-4o` wired in config.py but not needed. |
| 2 | 2026-09-10 | Benchmark stored as ONE file per base case with `perturbation_pair` and `noise_variant` nested, i.e. 36 files = 108 cases. | Matches `benchmark_case_template.json` exactly; PLAN.md's "108 files" read as 108 cases. Eval harness expands each file into 3 inputs. |
| 3 | 2026-09-10 | Discriminator id prefixes fixed: `hx_` history, `exam_` exam, `fn_` functional, `pocus_` POCUS. | Satisfies schema pattern `^[a-z]+_[a-z0-9_]+$`; type is recoverable from id. |
| 4 | 2026-09-10 | `discriminator_ids.json` schema: `{"ids":[{id,title,type,presentations[],differentials[],indication_tags[],expected_evidence,anchor_source,priority}]}`; `priority` 1 = extract first. | PLAN.md lists the required fields; `anchor_source`/`priority` added so extractors and the cardiology-first batch order are explicit. |
| 5 | 2026-09-10 | Publisher full-text sites (jamanetwork.com) return 403 to the sandbox; extractors use PubMed/PMC/Europe PMC/Crossref for quotes and locations, and record `location` from abstract/PMC tables. | Bot-blocking on the publisher side; PubMed abstracts of RCE articles carry the pooled LRs. Owner verification uses the primary. |
| 6 | 2026-09-10 | POCUS: extracted, `pocus` type disabled in retrieval (`POCUS_ENABLED = False` in config.py) until owner flips DAY1_FREEZE.md. | Owner instruction at kickoff. |
| 7 | 2026-09-10 | Literature lookups use Bash `curl`/Python `urllib` against NCBI e-utils, PMC OA, and Crossref (all reachable after the Full switch); the WebFetch tool stays egress-blocked because its policy is fixed at session start. `tools/pubmed.py` wraps e-utils so every extractor uses the same auditable path. | Subagents verified reachable via shell; WebSearch works for discovery only. |
| 8 | 2026-09-10 | Merged 3 id drafts (75+50+46) → 148 ids after folding 5 true duplicates (melena, AAA POCUS, hypoglycemia history, calf asymmetry, crackles). Kept `exam_diastolic_murmur_new` (dissection AR) separate from `exam_new_regurgitant_murmur` (endocarditis/HF). | Different differential sets = different retrieval keys. Crackles folded because it is one maneuver with two target conditions → two `estimates[]` entries. |
| 9 | 2026-09-10 | Vocab NOT extended in Phase 0; drafters' wish-list (e.g. `bacteremia`, `upper_gi_bleed`, `spinal_epidural_abscess`, `hypothyroidism`, `aortic_regurgitation`, tags `urinary_catheter`, `central_line`, `recent_antibiotics`, `nsaid_use`) is presented at the owner gate as a single decision. | CLAUDE.md: extend only if a case clearly needs it; no benchmark case required a missing term (36/36 validate against current vocab). |
| 10 | 2026-09-10 | Phase 2 deterministic modules built early (store/retrieve/render/validate/bayes, 30 tests). Resolutions: cross-presentation candidates always appended after in-presentation ones and flagged; sort key `(cross_presentation, -score, id)`; validator checks each item's numbers against that item's own record (stricter than "any linked record") so `block()` can attribute violations; ALL LLM-origin strings (rationale, chief_complaint, missing_features) pass through `strip_numbers`; `numbers_in_record` skips bookkeeping keys (record_version, source_index, timestamps) but includes source year; records whose `tier` disagrees with their folder are rejected; verified wins over extracted in the index; `"85%"` and `0.85` are distinct numbers (no derivation). | Each choice is the more auditable one; see CLAUDE.md non-negotiables. |
| 11 | 2026-09-10 | Bedside/off-bedside boundary for benchmark targets: medication review / MAR reconciliation = bedside HISTORY (it is asking what the patient takes); SpO2, telemetry, I/Os, weights, fluid-balance chart review = off-bedside MONITORING; POC glucose and bladder scan = off-bedside (lab/imaging). Applied in eval classifier and in case targets. | Rubric lists monitoring as off-bedside; the classifier must be deterministic, so the rule is written down once. |
| 12 | 2026-09-10 | Canonical target phrasing adopted from critic tables (e.g. "Orthostatic vital signs", "Bladder percussion/palpation for retention", "Pronator drift", "Murphy sign", "HINTS exam") so free-text targets map deterministically in Phase 3. | Free-text targets stay free text (spec), but consistent anchors cut mapping ambiguity. |
| 13 | 2026-09-10 | Compound targets (one item naming 2–4 maneuvers) are allowed; in Phase 3 a target maps to a SET of record ids and is "present" if ANY mapped id is on the card. Authors split items only where components have different clinical roles. | Keeps 3–6 targets per case; matcher rule is simple and auditable. |
| 14 | 2026-09-10 | `must_have` may point at a maneuver whose record will be `not_quantified` ONLY when it is guideline-standard and indispensable for that case (e.g. medication review in a hypotensive patient on new antihypertensives); where a literature-backed alternative exists, that one carries `must_have`. Flagged for owner at gate. | CLAUDE.md makes not_quantified first-class; case_author.md asks for literature-defensible must_haves; this reconciles both. |
| 15 | 2026-09-10 | Unified canonical target phrasing (supersedes the two critics' tables where they differ): "Orthostatic vital signs (supine to standing at 1 and 3 min)"; "JVP" verbatim; "Abdominojugular reflux"; "Calf circumference asymmetry >3 cm"; "Medication review (MAR): …"; "Bladder percussion/palpation for retention"; "Pronator drift"; "HINTS exam (head impulse, nystagmus, test of skew)"; "Dix-Hallpike maneuver"; "Murphy sign"; "CAM delirium screen (attention: months backward)"; "Neck stiffness, Kernig, Brudzinski, jolt accentuation"; "Pulsus paradoxus"; "S3 gallop"; "Bibasilar crackles" / "Focal crackles". | One string per maneuver across all 36 cases. |
| 16 | 2026-09-10 | LLM modules built (llm/parser/rank/pipeline/api; 54 tests offline via FakeLLM; live startup_check resolved `gpt-4.1`). Resolutions: parser weights clamped/deduped/rescaled so top=1.0; zero differentials → ParserError (sparseness lives in `missing_features`); POCUS disabled → `pocus` key omitted and stray pocus ids dropped with a log line; candidate summaries sent to the ranker strip digits from title/changes_what and `RankerNumberLeak` guards it; parser-vocab or ranker-containment failure → HTTP 502 with a "Withheld" empty card (never a guess); strict json_schema with json_object fallback + local jsonschema validation on every reply. | Enforces CLAUDE.md non-negotiables in code; see bedside_brief/README_pipeline.md for file:function map. |
| 17 | 2026-09-10 | Critic re-check (26 pass / 10 fix) applied verbatim by coordinator: 1 off-bedside term removed from a must_have (syncope_003), fever_003 #3 demoted (was 3/3 guideline-only and pre-answered in p1), 9 `expected_change` strings fixed so they never say an already-must_have item "becomes" must_have. Rule recorded: in underspecified cases `must_have` = whatever resolves the branch point; a maneuver testing a single branch is not must_have. | Unmeasurable transitions would silently zero the perturbation-responsiveness metric. |
| 18 | 2026-09-10 | OWNER GATE (Phase 0) rulings: (1) 148 ids approved + 6 palpitations ids to add; (2) vocab: six cross-listings of existing terms only (see vocab.json `_changelog`), no new terms/tags, revisit after Phase 3; (3) must_have kept on guideline-only maneuvers; eval reports must-have recall stratified quantified vs not_quantified; (4) bedside boundary confirmed; eval adds a sensitivity analysis with MAR review counted off-bedside; (5) compound targets: split when components are distinct constructs; ANY-rule only for same-construct compounds (e.g. meningeal signs); (6) POCUS extracted, disabled; (7) chest_pain_002 re-branched to pneumothorax; (8) freeze #1 after these edits. | Owner decisions 2026-09-10. |
| 19 | 2026-09-10 | Validator target cap raised 6 → 8 per case, only to accommodate compound splits mandated by gate ruling 5; authors keep ≤6 where possible. | Splitting must not force dropping content. |
| 20 | 2026-09-10 | Store units: sensitivity/specificity/prevalence are PROPORTIONS (0–1); LRs are ratios. `tools/normalize_units.py` converts percent entries and appends a note; validator rejects sens/spec > 1. Renderer shows stored values verbatim (no percent conversion) so the number-validator never needs a derived form. | Wave-1 extractors disagreed (one used percents). |
| 21 | 2026-09-10 | PENDING OWNER (batched, non-blocking): `exam_ascites_flank_dullness_fluid_wave` and `exam_shifting_dullness` share one evidence base (Simel 1988 / RCE 1992); both extractors flagged it. Proposal: fold into `exam_shifting_dullness` with fluid wave / flank dullness as separate `estimates[]` rows at verification; the id list is gate-approved so I have not touched it. | Avoids two cards for one maneuver family. |
| 22 | 2026-09-10 | **FREEZE #1** = commit `39a698e5e79a5b6eddebf090caa3f6c8a1b16f7b` (36 cases, 108 inputs, 257 targets, 102 must-have, 3 underspecified). Hash stamped into every case file; `tests/test_freeze.py` fails the suite on any drift except `reference_targets_mapped` (freeze #2). | Owner instruction "Then freeze #1" after rulings 1–7; critic split re-check recommended freeze after 11 edits, all applied. |
| 23 | 2026-09-10 | Red-team wave 1 (24 ids): 0 fabricated numbers; 1 direction error, 4 range-as-point values, ~9 wrong author lists, 7 composite findings credited to single-sign records. Fixes applied for wave 4+: extractor prompt gains DIRECTION / RANGES / COMPOSITES / ONE-NUMBER-MINIMUM / PROGNOSTIC / AUTHORS-from-tool / NOTES-DISCIPLINE / STATUS rules; validator warns on empty estimates; diff report adds AUTHOR MISMATCH (live) and COMPOSITE DUPLICATE (same PMID+value under two ids). Wave 1–3 packets are NOT edited — the owner fixes at verification with the red-team flags shown in verify_ui. | Packets are the extractors' independent record; fixing them post hoc would blur the A/B audit trail. |
| 24 | 2026-09-10 | Eval harness built (eval/: cases, classify, item_parser, arms, mapping, metrics, judging_export/import, error_analysis, report, run_all; 55 tests). Resolutions: arm C prompt sent verbatim as the user message inside a JSON wrapper (client speaks JSON only); target presence for arm C = mapped ids first, else deterministic text match (≥50% content tokens or any compound component), applied identically to all arms; `partially_quantified` counts as quantified in the must-have strata, unmapped targets form a third stratum kept in the denominator; perturbation responsiveness uses explicit `expected_change_targets` (sidecar file, so frozen cases don't change) else a logged Jaccard-change fallback; unclassified free-text items count as not-bedside; a missing `benchmark/target_map.json` yields a PROPOSED map that is written but not used. Figure is hand-written SVG (matplotlib absent). | Keeps every metric deterministic and traceable; nothing in frozen files changes. |
| 25 | 2026-09-10 | Red-team wave 2 (34 ids): 110/110 quotes verified, 0 fabrication; recurring composite-for-item substitution (9 ids), 12 direction issues, 11 status over-claims. Added to extractor prompt: THRESHOLD, DIRECTION SELF-CHECK, REVIEW TABLES (cite primary PMID), SEARCH PMC FIRST, QUOTE CAP 35 with CI. Validator warns on LR ≤ 0. Verifier guidance: merge complementary operator-expertise splits (A DVT / B lung sliding); `hx_palpitations_before_syncope` A packet narrative contradicts its LR. | Same rationale as #23; packets untouched. |
| 26 | 2026-09-10 | **FREEZE #2** = commit `28a5f2617dcd474602491e71823338316f18bc2d`: `reference_targets_mapped` filled in all 36 cases from `benchmark/target_map.json` (216/258 targets mapped, 92/101 must-have; 42 unmapped targets listed in the map's `_unmapped` = library-coverage gaps, e.g. wound inspection, last-known-well time, collateral history, spinal percussion, pneumothorax onset history). Correction to #22: harness tally is 258 targets / 101 must-have. From here cases and targets are read-only; product fixes re-run the frozen set. | Freeze protocol step 3. |
| 27 | 2026-09-10 | Parser dry-run on frozen inputs exposed (a) OpenAI 429s when two live processes ran concurrently → `LLMClient._call` now retries 429/5xx with exponential backoff (≤6 attempts); (b) the model emitting indication tags outside vocab ('smoker', 'afebrile', 'aaa') → parser does ONE corrective re-parse with the violation fed back, then still raises `ParserVocabError` (never silently drops). Product fix made before any A/B evaluation run, so no re-run comparison is owed. | Hard-failing the whole card on a stray tag would zero arm A for a formatting slip, not a clinical one. |
| 28 | 2026-09-10 | Red-team wave 3 (37 ids): 107/108 quotes verified, 0 fabricated numbers; 1 invented first author (`pocus_bladder_volume` B) and a pasted wrong author list across 4 B packets (Kharbanda→Becker); 5 empty estimates to delete at verification. Prompt adds PREVALENCE / AGE GROUP / ONE-ANALYSIS-ONE-ESTIMATE / NOTES rules. | Same handling as #23/#25; verifier fixes at promotion with flags shown in verify_ui. |
| 29 | 2026-09-10 | Parser contract gains `underspecified: bool` (strictly defined: the one-liner lacks timing/trigger/context needed to rank at all; expected false for most inputs); `missing_features` is emitted only when true and must be 1–3 history questions, never exam/lab items. Render shows the "ask first" line only when `underspecified` is true. Reason: dry-run after the vocab fix showed ask-first firing on 61/102 well-specified inputs because the model listed desired exam findings as "missing features"; it fired correctly on 6/6 underspecified inputs. Product fix before any A/B run. Presentation mismatches (7/108, e.g. hypotension_003 → dyspnea because of hypoxemia) are left as-is: presentation is only a filter with cross-presentation fallback; impact to be read from error analysis. | DAY1_FREEZE: "ask first" only for a missing feature that would change ranking. |
| 30 | 2026-09-10 | `underspecified` recalibrated to judge the PRESENTING COMPLAINT, not patient background (age/ward/admission dx/devices are background and do not make a vague complaint specified). Measured on frozen inputs: ask-first fires 4/6 underspecified, 0/6 well-specified (before the flag: 6/6 and 61/102). Residual disagreement is weakness_003 ("too weak to walk today"), a genuine judgement call, left for the error analysis to report. | Two calibration rounds were run against frozen benchmark INPUTS (never their targets). |
| 31 | 2026-09-10 | **DISCLOSURE (limitation, must appear in the abstract):** the parser prompt's `underspecified` rule was calibrated by observing behaviour on the frozen benchmark one-liners, so ask-first accuracy is not an out-of-sample estimate. No target, expected_change, or should_not_recommend text was consulted, and no other prompt was tuned this way. Tuning stopped after two rounds. | Honesty about development-set contamination; the alternative (tuning on invented inputs) is deferred to the poster. |
| 32 | 2026-09-10 | code-reviewer (partial, killed by usage limit) tightened three real gaps, kept: `extraction_notes`/`verification_notes` no longer widen the card number allow-list (notes routinely quote ORs/RRs deliberately NOT entered as estimates); `block()` now also strips violating card-level fields (e.g. `ask_first`) so a blocked card re-validates clean; `tests/test_freeze.py` gains a byte-identical guard against freeze #2. 202 tests green. | Each is a strengthening of a CLAUDE.md non-negotiable. |
| 33 | 2026-09-10 | code-reviewer audit complete: all 10 non-negotiables PASS, verified by executing adversarial checks (LLM rationale carrying "LR+ 12.3, PMID 1234" reaches the card with every digit stripped; ranker cannot select an extracted-tier id; `/record/{id}` refuses non-verified; retrieval byte-identical over 3 runs; no key or Authorization header ever logged). Card-tier enforcement is redundant across retrieve/render/validate/api. Two tests added; one flagged issue fixed: the `tools.validate` import fallback in store.py now logs instead of failing silently. | CLAUDE.md gate: code-reviewer passes each module before merge. |
| 34 | 2026-09-10 | Red-team wave 4 (58 ids, 116 packets): **0 fabrication, 0 author mismatches across 34 verified PMIDs (vs ~9 in wave 1), 0 unlabelled composites, 0 range-as-point, 0 literal LR=0** — the hardened prompt eliminated the wave 1–3 failure modes. Residual: 1 unlabelled prognostic row, 16/58 status mismatches (one extractor keeps a borderline estimate the other declines), and search-completeness asymmetry. Added a mechanical PROGNOSTIC-UNLABELLED check to `tools/diff_report.py` per the red-team's suggestion. | Closes the extract→red-team loop; remaining differences are owner judgement calls, which is what the verification queue is for. |
| 35 | 2026-09-10 | Six "cheapest upgrade" ids where one packet is `not_quantified` but the PAIRED packet carries a real, located, verified number: `exam_chest_hyperresonance`, `exam_line_exit_site_erythema_purulence`, `hx_steroid_exposure_withdrawal`, `exam_kussmaul_sign`, `hx_recent_antibiotics_new_diarrhea`, `exam_tense_distended_abdomen_iah`. Surfaced at the top of the owner's queue: promote from the packet that found the number. | Highest verified-record yield per minute of owner time. |
| 36 | 2026-09-11 | Owner verified 153/153 (86 packet B, 67 packet A; 9 estimates excluded, 1 corrected, 0 rejected). `tools/pull_decisions.py` bug fixed: when every estimate is excluded the record becomes `not_quantified` and must keep its sources, which the source-pruning step was deleting. Store: 153 records, 349 estimates, 12/12 presentations, 22.2% not_quantified, validator 0 errors. | Gate 2 closed. |
| 37 | 2026-09-11 | **Post-promotion fix package applied** (owner: flip-a / label / rewrite / apply). 12 estimates deleted across 10 records (6 range-as-point or reversed-design, 6 with no numeric value); 2 fabricated citations corrected against PubMed — "Beacock CJ" → Taylor DL et al. (PMID 30325783) and the Kharbanda 2005 author list → Becker T et al. (PMID 17192449), each present in exactly one record; `pocus_bladder_volume` re-promoted from extractor-A (400 mL retention threshold, right direction) instead of B (positive state = PVR <100 mL, inverted); `exam_abdominal_rigidity_guarding` estimates labelled "(pediatric)"; `exam_shock_index` outcome rows prefixed `PROGNOSTIC:`; `fn_single_breath_count` review-table row replaced by the traced primary (Kukulka 2020, PMID 32447763, adds spec 0.60); `exam_murmur_late_peaking_systolic` gains extractor-A's row for its own sign (LR+ 3.7 / LR− 0.2), without which the record quantified only component findings; `hx_palpitations_before_syncope` interpretation rewritten to lead with LR+ 0.74 rather than assert a rule-in direction the number contradicts. Store: 153 records, 337 estimates, 83 quantified / 33 partial / 37 not_quantified (24.2%), validator 0 errors (warnings 14 → 8), 204 tests green. Every change is stamped into the record's `verification_notes` as an `[owner fix]` line; the two bad citation strings survive only there, as the audit trail. | Defects the red team had already flagged were promoted with the packet that carried them; the owner approved each fix individually rather than accepting a blanket packet flip, which for 4 of the 9 would have discarded good estimates. |
| 38 | 2026-09-11 | Note for the limitations paragraph: defects 1–3 of the nine (`exam_carotid_upstroke_delayed`, `exam_chest_wall_tenderness_reproducible`, `exam_murmur_late_peaking_systolic`) are the **same failure mode against the same source** — Etchells 1997 (PMID 9032164) reports only between-study ranges (2.8–130, 8.0–101, 0.05–0.10) and extractor-B took each lower bound as a point estimate. Report as one recurring error, not three independent ones. Also fixed a real bug in `tools/validate.py`: the empty-estimate check tested the stat dict (a non-empty dict is truthy) rather than its `.value`, and ignored `prevalence`, so it had never detected an empty row. | Accurate error accounting; the validator gap means earlier "0 errors" claims did not cover empty estimates. |
| 39 | 2026-09-11 | Product fix before the reportable A/B/C run: `rank.candidate_summary`'s number-leak guard serialised the digit-free candidate rows with `json.dumps` at its default `ensure_ascii=True`, so a real arrow (U+2192) in `interpretation.changes_what` became `→` and the scan read **2192** as a leaked number — a hard failure for every card retrieving one of the 10 records that write "finding → action" (first seen on `aki_002_p1`). Fixed with `ensure_ascii=False`; regression test added (verified to fail without the fix). The payload actually sent to the model (`rank.py:104`) is left escaped, so model input is unchanged and no earlier run is invalidated. The card-level number allow-list walks the record tree by key rather than a serialised blob, so it never had this bug. | A guard that rejects valid cards on a typography character is a false positive, not safety; the fix narrows it to what it was meant to catch. |
| 40 | 2026-09-11 | **Resume treated a stored failure as a finished case.** `run_arm` never raises — a case that blows up is saved as an output with `error` set and no items — but `run_case_arm` skipped any (arm, case) whose file merely *existed*. So the 17 arm-A cases that died on the `RankerNumberLeak` bug (all of `aki_*` and `ams_*`) were skipped, not retried, when the run resumed after the fix, and would have entered the reported metrics as genuine empty cards: 17/108 inputs, 15.7% of arm A, concentrated in two presentations. Found by aggregating the arm-A outputs rather than trusting the log, which was clean because the failures predated the restart. Fixed: resume reloads only successful outputs and retries a stored failure, logging that it is doing so; the 17 stale files were deleted and will be regenerated. Regression test verified to fail against the unpatched resume. | A resumable runner that cannot distinguish "done" from "failed" turns any transient error into a silent, permanent hole in the results. |
| 41 | 2026-09-11 | **Bedside-share sensitivity analysis added, primary metric unchanged.** Pre-specified (#24), an `unclassified` item counts as not-bedside and stays in the denominator. On the frozen run 496 of arm C's 3,099 items (16.0%) are unclassified, and inspection shows most are not recommendations at all: 113 section headers ending in ":", 30 citation lines, and a long tail of commentary attached to the item above ("Sensitivity ~94%, specificity ~89% for delirium diagnosis", "Can detect wall motion abnormalities"). Counting those against C measures its prose style rather than what it told the clinician to do. `metrics.bedside_share_classified` reports the same three modes with unclassified items dropped from both sides: C 60.1% → 71.6%. The pre-specified `bedside_share` is reported first and unchanged; arms A and B have zero unclassified items so are unaffected. | A reviewer will ask whether the gap is parsing or substance; the answer should be in the table, not invented at the poster. |
| 42 | 2026-09-11 | **Also for the abstract: arm A's 100% bedside share is true by construction** — its store contains only bedside discriminators, so the metric cannot come out any other way. It is a design property, not a finding, and the comparison sentence should be carried by arm C's number (how much off-bedside work a generic LLM volunteers) and by must-have recall, where arm A can and does lose. | Avoids a near-circular headline that a reviewer would rightly discount. |
| 43 | 2026-09-11 | **Safety failure mode found on the frozen run, and the pipeline fix for it.** In `hypotension_002` (all three variants) arm A recommended orthostatic vitals for a patient whose supine SBP is under 90 — exactly the case's `should_not_recommend` item — and rendered the record's own contraindication underneath it. `exam_orthostatic_vitals.safety_scope.do_not_use_when` already says "Do not attempt standing measurements in a patient who is already hypotensive"; the field was reachable from one place in the whole codebase, `render.py`, which prints it. Nothing read it. Fix: `candidate_summary` now passes the digit-stripped `do_not_use_when` to the ranker as `avoid_when`, and the system prompt instructs it not to choose a candidate whose condition the patient meets. The ranker is the only stage that sees both the manoeuvre and the patient. No verified record changes, no number reaches the model (the field goes through `strip_numbers` like every other), and the candidate allow-list stays closed — the test asserts the key set is a subset of the six permitted keys. Arms B and C do not rank, so the re-run is arm A only and is reported as a second run alongside the first (PLAN Phase 3). | PLAN Phase 3: "fix high-impact failure modes in pipeline only, re-run frozen set, report both runs". A card that prints a contraindication while recommending the manoeuvre is the worst kind of failure for a bedside tool. |
| 44 | 2026-09-11 | **The parser was stripping the patient's own measurements, and that was the real cause of the safety failure.** `parser.py` prompts "chief_complaint: no numbers" / "modifiers: (no numbers)" and then runs `strip_numbers()` over every returned field. Measured on the frozen set: 27 of 36 one-liners state a vital sign and **0 of 108 parses kept one**. `BP 88/54` at rest became chief_complaint "lightheadedness and hypotension **on standing**", under which `exam_orthostatic_vitals.safety_scope.do_not_use_when` ("do not attempt standing measurements in a patient who is already hypotensive") does not apply — so the ranker chose it, correctly, for the patient it was described. This is the no-numbers containment rule enforced one stage too early: the card is already protected twice downstream and independently (`render.py` strips every card-level string again; the validator blocks any number absent from a linked verified record), so the parse-level strip bought no containment and cost the ranker the patient's state, which in acute medicine is mostly numeric. Fix: `observed_values`, copied verbatim from the one-liner, not digit-stripped, passed to the ranker and never rendered. Result: all three variants of `hypotension_002` now decline to stand the patient, and arm A's `should_not_recommend` hit rate goes 2.8% → 0.0%. | The distinction the architecture actually needs is patient facts (from the clinician, may be numeric, never rendered) versus evidence claims (only from verified records). Conflating them is what blinded the ranker. |
| 45 | 2026-09-11 | **`should_not_recommend` matcher bug — three of six run-1 "safety hits" were false positives.** `_snr_hit` required `len(a & b) >= 2` and `max(overlap(a,b), overlap(b,a)) >= 0.6`. Prohibitions are written short and card items long, so the asymmetric overlap decides: "NSAIDs for muscle pain" is three content tokens, and the card item "Muscle pain/weakness, prolonged immobilization or exertion, dark cola-colored urine" shares `muscle` and `pain` → 2/3 = 0.67, a hit, with `nsaid` nowhere in the item. That item is correct rhabdomyolysis screening in a found-down patient with Cr 3.4. Fixed: below four content tokens a prohibition must be wholly contained in the item; at four or more the 0.6 rule stands. Corrected arm-A hit rates: run 1 5.6% → **2.8%**, run 2 4.6% → **1.9%**, run 3 0.9% → **0.0%**. Both the pre-specified and corrected numbers are reported. | A metric that penalises the right recommendation for sharing two common words with a prohibition is measuring vocabulary, not safety. |
| 46 | 2026-09-11 | **Arm B is the control that makes the attribution clean.** It shares the parser but does not rank. Across runs 1→3 arm B is flat (must-have recall 66.0% → 66.0% → 65.7%; quantified stratum 66.7% throughout; perturbation 63.0% → 63.4%), so arm A's degradation over the same runs (recall 69.0% → 67.7%, perturbation 68.1% → 57.9%, items per card 6.15 → 5.50) is a **ranking** effect, not information lost in the parse. Hypothesis: with concrete patient state now visible, the `avoid_when` line on all ~23 candidates per case makes the ranker over-cautious — it declines items and ships shorter cards. Run 4 ablates it: `observed_values` kept, contraindications hidden (`BB_RANKER_CONTRAINDICATIONS=0`, arm A only; B and C reused). If safety holds at 0.0% while recall and perturbation recover, that is the configuration to ship. | The ablation is cheap (one arm), decisive, and reportable — a reader can flip one environment variable and reproduce it. |
| 47 | 2026-09-11 | **The two fixes form a 2×2 and the result is a real safety/performance trade-off, not a free win.** Arm A, frozen set, corrected `should_not_recommend` matcher: baseline (neither) recall 69.0% / perturbation 68.1% / snr 2.8%; `avoid_when` only recall 68.3% / 63.9% / 1.9%; **`observed_values` only recall 71.0% / 69.4% / 2.8%**; **both recall 67.7% / 57.9% / 0.0%**. Showing the ranker a contraindication costs performance, and costs *more* once vitals make the contraindications concrete (perturbation 68.1 → 63.9 with avoid_when alone, 69.4 → 57.9 once vitals are added) — every one of the 153 records carries a `do_not_use_when`, so the model weighs one against all ~23 candidates per case. But only the combination reaches zero violations; vitals alone still stand the patient at 88/54 in all three `hypotension_002` variants, so the explicit contraindication is load-bearing for that case. Arm B (shares the parser, does not rank) is flat across all four runs, which is what licenses attributing the whole effect to ranking. Configuration to ship is an owner call on clinical risk tolerance, not a metric-maximising one. | A 2×2 ablation that a reader can reproduce with one environment variable is a better result than any single tuned setting, and it states the cost of the safety behaviour instead of hiding it. |
| 48 | 2026-09-11 | **Owner decision on the 2×2: ship `observed_values` with contraindications OFF** (best measured performance — recall 71.0%, quantified stratum 74.4% vs arm C's 72.6%, perturbation 69.4%), accepting 3 violations of 108 inputs, all three `hypotension_002` variants recommending orthostatic vitals at supine 88/54. Concern was stated before the decision and reaffirmed by the owner. `config.RANKER_CONTRAINDICATIONS` now defaults to `"off"` and the abstract's arm A is this configuration; the violation count and what it is must appear in the limitations, not only in the table. | Owner's clinical risk call. The alternative (`"all"`) reaches zero violations but loses ten points of perturbation responsiveness and puts arm A behind arm C on overall recall. |
| 49 | 2026-09-11 | **Third mode added and run as run 5: `RANKER_CONTRAINDICATIONS="relevant"`.** Rather than attaching every record's caveat to all ~23 candidates, `bedside_brief/patient.py` reads the vital signs the one-liner actually stated (`observed_values` → sbp/hr/spo2/temp via narrow regexes), converts them to flags (hypotensive <90, bradycardic <50, tachycardic >120, hypoxic <92, febrile ≥38.0), and shows a `do_not_use_when` only when it names a state the patient is measurably in. Deterministic, unit-tested, and it errs towards hiding: no readable measurement means no caveat shown. On the case that matters — supine BP 88/54 against "do not attempt standing measurements in a patient who is already hypotensive" — the gate fires. Hypothesis under test: zero violations at run-4 performance. If it holds, promote it over `"off"` and revisit #48 with the owner. | The safety behaviour was costing performance because it was applied indiscriminately, not because it is inherently expensive; a deterministic relevance gate is also a better fit for an architecture whose thesis is that retrieval and validation are code. |
| 50 | 2026-09-11 | **Run 5 result: the narrow gate gets zero violations back for about a tenth of what the blanket version cost.** Arm A, frozen set: run 3 (caveat on all ~23 candidates) recall 67.7% / perturbation 57.9% / 0 violations; run 4 (no caveat, shipped) 71.0% / 69.4% / 3 violations; **run 5 (caveat only where the patient's stated measurements meet it) 70.0% / 65.3% / 0 violations**. So the safety behaviour was never intrinsically expensive — it was expensive because it was applied to every candidate. Against run 4 the gate costs 1.0 point of must-have recall, 2.2 on the covered stratum and 4.1 of perturbation responsiveness, and removes all three `hypotension_002` violations. On the covered stratum arm A is then 72.2% against arm C's 72.6% (tied) rather than 74.4% (ahead). Recommended to the owner as the shipped default over `"off"`. | Promised at #49: if the gate reached zero violations at comparable recall it should displace `"off"`. It did, and a deterministic gate is also the version whose behaviour a reader can check, unlike a prompt instruction. |
| 51 | 2026-09-11 | **Owner decision, revisiting #48: ship `RANKER_CONTRAINDICATIONS="relevant"`** (run 5) rather than `"off"`. Zero `should_not_recommend` violations at recall 70.0% / covered stratum 72.2% / perturbation 65.3%, against vitals-only's 71.0% / 74.4% / 69.4% with 3 violations. Arm A therefore ties arm C on the covered stratum (72.2% vs 72.6%) instead of leading. Accepted because a ~2-point stratum difference sits inside run-to-run variance we have not quantified, while "recommends standing a patient at supine 88/54" is a defect a reviewer and a clinician would both stop on. The abstract's arm A is run 5; the limitation to state is now the **gate's reach**, not the violation count — it fires only on states readable from a stated measurement, so non-numeric contraindications ("too dizzy or weak to stand safely") are not enforced, and zero violations on 108 synthetic inputs is not zero risk. | #48 was decided before the narrow gate existed; #49 committed to bringing it back if the gate reached zero violations at comparable recall, which it did. |
| 52 | 2026-09-12 | **Gate 3 labour saving, with no AI judgement anywhere.** The owner asked for AI-suggested scores to approve. Declined as designed, because judged relevance is the abstract's one non-mechanical metric: pre-filled labels anchor the judge, and the suggester would be the same model family as arm A's ranker, so arm A's output would be partly graded by its own kind — and even blinded, a model may rate terse record titles differently from arm C's verbose prose, which is a differential bias between the arms being compared. Instead: (a) **grouping** near-identical recommendations at `eval.mapping`'s own 0.6 threshold — the same notion of "the same item" already used for target presence — so each distinct recommendation is judged once and the score applies to every arm that made it: 525 items → 427 judgements (19% fewer), and it removes the judge-inconsistency noise of scoring the same recommendation differently under different arms; (b) a **"not an item"** label, the judge's own escape hatch for headings, citation lines and asides, excluded from the relevance denominator with the inclusive denominator also reported; (c) a one-tap **"mark the remaining N irrelevant"** per case, which is the owner's judgement expressed once. | Everything that saves the owner time here is either deterministic text grouping or their own judgement; nothing is a model's opinion about relevance. |
| 53 | 2026-09-12 | **Rejected a labour saving I had already proposed**: dropping the 69 queue items the deterministic classifier calls `unclassified` (13% of the queue, all arm C). Reading all 69 showed the bucket mixes genuine artifacts ("Collins SP, et al. JAMA. 2020;324(5):486-499", "Specificity: >90% for MI", "Already noted at 96%") with **real arm-C recommendations the item parser could not place** ("Cerebellar: finger-nose-finger, heel-shin, rapid alternating movements", "Bedside pregnancy test", "IVC diameter/collapsibility"). Excluding them wholesale would have discarded real recommendations from the comparison arm, in a direction that cannot be predicted. The "not an item" label puts the same decision in the judge's hands instead. | The `unclassified` bucket is a parser failure signal, not a content category; treating it as one would have quietly biased the arm comparison. |
| 54 | 2026-09-12 | **Standalone blinded judging packet as a PDF** (`tools/judging_packet.py`, 31 pages, A4): the rubric, each case's one-liner, and all 427 grouped recommendations with tick boxes. Built for a **second independent rater**, which is worth more than the convenience: the study's stated limitation is a single author who is also sole verifier and judge, and two raters on the same blinded queue turn that into a reported agreement figure. Blinding verified mechanically — none of the 525 item uids appears in the document, and every occurrence of the word "arm" in it is anatomical ("arm drift", "radiating to both arms"). The tool never reads `judging_key.json`. Rendered by printing HTML through the headless Chromium already installed for the browser checks; reportlab would have fought us for the same typography. | A PDF of the interactive page would have been a picture of buttons. What a colleague needs is a document that stands alone and can be scored on paper. |
| 55 | 2026-09-12 | Grouping now drops a wording variant identical to the one shown (38 of 73 merged groups were printing "also worded as:" above a copy of their own line, because two systems emitted the same string). The `*` marker carries the merge instead. Fixed in `tools/seed_judging.group_queue`, so the iPad page and the PDF inherit it together. | Found by rendering the packet and reading it, not by testing it. |
| 56 | 2026-09-12 | **Judged relevance is graded bedside-blind — correcting the rubric advice I gave in #52.** I had told the owner to grade a lab, ECG or CT as irrelevant "because this is a bedside card". Wrong, for two reasons. (a) **Redundancy**: `bedside_share` already measures bedside-ness mechanically in three classifier modes plus a sensitivity analysis, over all 3,099 arm-C items rather than the 525 in the judging queue, so grading it again in the human metric would duplicate a number we already have precisely and waste the only judgement a machine cannot make. (b) **Circularity**: arm A emits only bedside items by construction, so if off-bedside implied irrelevant, arm A's judged relevance would be near-guaranteed high and arm C's mechanically capped at roughly its bedside share — importing into the human metric exactly the near-circularity flagged against the bedside-share headline in #42. The two metrics now answer different questions: did it stay at the bedside (mechanical), and was what it said any good (judged). Operational rule in both the page and the PDF: ignore where the recommendation happens, ask only whether you would want it done for this patient today. | The likely result is also the sharper claim: if arm C scores well on relevance, the finding is that the unconstrained model's advice was largely sound but not short, bedside or verifiable — which is far more defensible than calling it irrelevant. |
| 57 | 2026-09-12 | **Judging page did nothing on the owner's iPad; cause was four defects of mine, none reproducible by the happy-path stub I had tested with.** (1) `loadCase` swallowed every error, so a failed case read rendered a permanent "Loading this case…" with chips that stayed tappable and did nothing — indistinguishable from a dead page. (2) Startup fired **12 concurrent** `doc().get()` calls (`for (const id of ids) loadCase(id)`), which a rate limit or a per-call consent gate rejects, including the current case's; combined with (1), invisibly. Now the current case loads first and awaited, the rest sequentially in the background. (3) `window.confirm()` in the bulk action: artifacts render in a sandboxed iframe where a modal is blocked and `confirm()` simply returns false, so that button could never have worked. Replaced with a two-tap arm/confirm. (4) A failed read of `judging_scores` was swallowed and scoring continued from an empty set — the next tap would `set()` the case document and **destroy scores already recorded**. Now a hard stop with an explanation. Added an always-current diagnostics line (store state, cases loaded, failures with their error codes, last save error) so the next failure is legible without me. `tools/web/check_judge_failures.py` exercises all three failure paths; WebKit could not be installed here (missing system libraries), so iPad-specific behaviour remains untested and the page is written to fail loudly instead. | A stub that only models the success path certifies nothing. Every one of these four was a silent-failure design choice, and the one that mattered most was the one that could have deleted the owner's work. |
| 58 | 2026-09-12 | **Only `palpitations_001` failed to open, and the cause was the fix from #57.** The stored document is byte-identical to the local one, all 34 groups well-formed, no duplicate ids, and it renders correctly under test — so not the data. The index says `palpitations_001` is `order: 1`, and #57 had made startup load the current case awaited and then prefetch the remaining eleven sequentially in the background: palpitations is therefore the **first background read**, issued immediately after the current case's, and the one most likely to be refused by a burst or rate limit. Fixed by dropping the prefetch (cases now load when opened — 2 reads on open instead of 13) and retrying a failed read three times with 400/1200/3600 ms backoff before surfacing it, so a transient refusal heals without the reader noticing. Verified against a stub that refuses that one document twice and then succeeds: the case opens after the backoff, 34 rows, no page errors. | The prefetch bought instant chip-switching and cost eleven reads on open. Load-on-demand is both cheaper and the reason the collision no longer exists. |
| 59 | 2026-09-12 | **The real cause, after three wrong diagnoses: the "Not an item" button had no pressed style, so tapping it changed nothing visible.** Recorded from the owner's own device (Safari 26.6, macOS, 1590×975) via the `judging_diag` collection: `palpitations_001` loaded fine (`already_loaded: true, groups: 34`), no read failures, no render throw — and its score document was at **version 34 with 11 valid grades already saved**. Nothing was broken in loading, rendering or saving. The CSS had pressed-state rules for `relevant`, `marginal` and `irrelevant` but none for `not an item`, the fourth grade added later, so that button set `aria-pressed="true"` (invisible) and kept its unpressed background — white in light mode, and in dark mode near-black text on near-black at 1.06:1, so the label vanished too. Palpitations is the case densest in headings and citation lines, which is where a reader reaches for that button constantly. Fixed with a `--nai` token, a `--pressed-fg` token replacing two overrides that were resolving by specificity rather than intent, and a rule per grade; all eight grade×theme combinations now change background and clear 4.5:1. | Three rounds of tests asserted `aria-pressed`, which was correct the entire time. An assertion about the DOM is not an assertion about what a reader can see. |
| 60 | 2026-09-12 | **Owner's completed judging packet read back: 427 of 427 rows graded, one grade each, none blank, 4 flags** — 214 relevant / 85 marginal / 57 irrelevant / 71 not-an-item. Marks are vector strokes inside the box outlines, not form fields or annotations, so `tools/read_judging_pdf.py` locates the five box rectangles per row and asks which contains a stroke; it also verifies alignment by comparing each row's printed text against the group it should hold, because an off-by-one would silently shift every grade in the document (alignment: ok, 427/427). **Provenance, for the disclosures:** the file arrives stamped "ASSISTANT DRAFT — FOR INDEPENDENT PHYSICIAN REVIEW" and named `assistant-judgments`, which I queried before using it; the owner confirms they drafted every grade and used an AI system only to check the packet for completeness, not to supply grades. The abstract's "author-judged" therefore stands, and the disclosure should say a tool verified completeness. | Using an assistant-stamped file as the author-judged metric without asking would have put a false claim in the paper; the stamp is the completeness checker's, and the grades are the author's. |
| 61 | 2026-09-12 | **Rubric correction scoped mechanically to 44 of 427 rows (10%).** DECISIONS #56 changed judged relevance from bedside-only to bedside-blind after the owner had already graded, so the affected population is exactly: groups whose items are all in off-bedside classifier categories (lab, imaging, ecg, monitoring, treatment, mar, consult) AND graded marginal or irrelevant. No judgement in the selection — a category filter plus the grade already recorded. Breakdown is **38 irrelevant, 6 marginal**, so the correction is larger than the owner's "troponin and ECG are marginal instead of relevant" suggested: under the old rubric they were mostly grading these irrelevant. Re-grade sheet (`tools/regrade_packet.py`, 7 pages) shows each row with its case's one-liner, full text, category, and its current grade pre-marked as *keep*, so only changes are ticked. Every call stays the owner's; I convert nothing. Caveat noted to the owner: a few of the 44 are there because the classifier mislabelled a history item ("Anticoagulation history", "Allergies" as `treatment`), and those simply keep their grade. **Direction disclosed in advance**: converting off-bedside items upward raises arm C's judged relevance more than arm A's, since C is the only arm producing labs and imaging. | The correction must not become my judgement, and the owner must know which way it pushes the result before making the calls. |
| 62 | 2026-09-12 | **Variant A rewritten and filled; Variant B retired in place.** Results now lead with the A-versus-B ranker isolation (judged relevance 72.2% vs 60.0%, prohibited items 0.0% vs 2.8%) rather than the A-versus-C contrast, and the A-vs-C material follows. Three framing corrections: the "store verification incomplete at submission" limitation is deleted (all 153 records were verified at gate 2, so it is now false); arm A's bedside share is stated as holding **by construction** rather than reported as a result, since arm A can only emit bedside items; and both post-freeze departures are disclosed in Limitations (the ask-first rule was calibrated on the frozen inputs, and relevance was re-graded bedside-blind after grading had begun bedside-only). Variant B is kept as a 49-word "SUPERSEDED, do not submit" note that records why its pre-specified fallback conditions were not met, instead of being deleted. Trimmed over six passes from 536 to exactly 400 words; `tools/number_audit.py` reports 32/32 placeholders resolved, 0 untraced, against run 5. | Leading with A-vs-B makes the strongest claim the best-controlled one — B shares the parser, the store and the retrieval order, so the difference is attributable to ranking alone, whereas A-vs-C confounds every layer at once. Keeping a limitation that the work has since closed would understate the store; reporting a design property as a finding would overstate the result. Deleting Variant B would erase the pre-specified fallback it was written to document. |
